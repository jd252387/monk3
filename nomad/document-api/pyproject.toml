[project]
name = "document-api"
version = "0.1.0"
description = "Document API service"
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.115.5",
    "uvicorn[standard]==0.32.0",
]

[tool.uv]
package = false

