quarkus:
  http:
    port: 8080
  swagger-ui:
    always-include: true

indexer:
  catalog:
    source: file
    file:
      config: config/catalog.json

monk3:
  search:
    material-type-field: material_type
    backends-file: config/backends.json
