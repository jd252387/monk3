quarkus:
  http:
    port: 8080
  swagger-ui:
    always-include: true

indexer:
  catalog:
    source: FILE
    file:
      config: config/catalog.json
      backends: config/backends.json

monk3:
  search:
    material-type-field: material_type
